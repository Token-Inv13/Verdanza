/** Loyalty wallet reads remain unavailable in normal application entrypoints. */
export const CAGNOTTE_READ_DISPLAY_ENABLED: boolean = false;

/** Loyalty credit use remains unavailable in the normal cart and checkout. */
export const CAGNOTTE_CHECKOUT_USE_DISPLAY_ENABLED: boolean = false;

/** Sensitive loyalty administration tools remain unavailable in the normal UI. */
export const CAGNOTTE_ADMIN_TOOLS_DISPLAY_ENABLED: boolean = false;
